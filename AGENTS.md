# AI Agent Development Guidelines (AGENTS.md)

このドキュメントは、AIアシスタント（エージェント）が `omni-sim` プロジェクトでコードを記述・修正する際に遵守すべき共通ルールを定めたものです。

## 1. アーキテクチャ原則

- **Bevy ECS 駆動**: 全てのドメインロジックは Component と System で表現し、密結合を避けてください。
- **ドメインと表示の完全分離**: 
    - `src/systems/` 以下のドメインロジックには、表示用文字列（日本語、絵文字）、`info!()`、`println!()` を含めないでください。
    - 表示が必要な場合は `SimulationEvent` を発行し、`src/plugins/cli_output.rs` や `tui_output.rs` で処理してください。
- **決定論的再現**:
    - `rand` クレートは使用せず、`seed × tick × index` 等を元にした決定論的なハッシュを使用して「ランダム」を制御してください。
- **固定 Tick 実行**: `FixedUpdate` スケジュールを使用し、システム間の実行順序を `.after()` 等で明示してください。

## 2. コーディング規約

### 2.1 マジックナンバーの禁止
- バランス調整に関わる係数（消費率、建造コスト、研究倍率など）を直接コード内に記述しないでください。
- 全ての定数は `src/config.rs` の `BalanceConfig` に集約し、`Res<SimulationConfig>` を通じて参照してください。

### 2.2 パフォーマンス最適化
- **O(N^2) ループの回避**: 二つのエンティティ間の衝突や相互作用を計算する際、全結合の二重ループを避けてください。
- **インデックスの活用**: `NationPlanetIndex` のような、逆引きや特定の関係性を高速化するインデックスコンポーネント/リソースを活用してください。

### 2.3 データモデル
- **適切な所有関係**: 技術（`TechnologyState`）や外交メモリ（`DiplomaticRelation`）は、**惑星（Planet）** ではなく **国家（Nation）** に紐付けてください。惑星は居住環境と資源産出に特化させます。
- **コンポーネントの分割**: 肥大化したコンポーネントは、不変データ（静的データ）と可変データ（動的環境など）に分割することを検討してください。

### 2.4 Bevy 名前衝突への対策
- `Name` コンポーネントを使用する際は、Bevy 標準の `bevy::prelude::Name` とプロジェクト独自の `Name` が衝突しないよう、明示的に型指定するか別名を使用してください。

## 3. ドキュメントの維持
- 大幅なアーキテクチャの変更や新しいサブシステムの導入を行った際は、必ず `docs/architecture/` 以下のドキュメントを更新してください。
- インターフェースの変更（`BalanceConfig` へのフィールド追加など）は、`docs/architecture/deep_dives/04_formulas_and_constants.md` に反映させてください。
